---
title: Dives
---

import certsData from '@site/static/certs.json';

export function DivesStats() {
  const statBoxes = [
    { label: 'Number of dives', value: '132' },
    { label: 'Total time', value: '102 hours, 56 minutes' },
    { label: 'Max depth', value: '39,5 meters' },
    { label: 'Max time', value: '71 minutes' },
  ];

  return (
    <div className='boxes'>
      {statBoxes.map(({ label, value }) => (
        <div className='statBox' key={label}>
          <div className='label'>
            <p>{label}</p>
          </div>
          <div className='value'>
            <p>{value}</p>
          </div>
        </div>
      ))}
    </div>
  );
}

export function DivesCards() {
  const cardBoxes = certsData;

  return (
    <div className='boxes'>
      {cardBoxes.map(({ title, id, date, logo }) => (
        <div className='cardBox' key={id}>
          <div className='label'>
            <img src={logo} height='25' className='logo'></img>
            {title}
          </div>
          <div className='id'>{id}</div>
          <div>{date}</div>
        </div>
      ))}
    </div>
  );
}

# Dives <img src='/img/dives.png' height='48' style={{verticalAlign: 'top', marginTop: '0.35rem'}} />
<DivesStats />

<br />

<p><a href='/dives.csv'>Download CSV</a></p>

<br />

<details>
  <summary>Show my certifications</summary>
  <DivesCards />
</details>