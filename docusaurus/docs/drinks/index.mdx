---
title: Drinks
---

import useGlobalData from '@docusaurus/useGlobalData';
import syrupsData from '@site/static/syrups.json';

export function DrinksStats() {
  const stats = useGlobalData()['drinks-stats']?.default;

  if (!stats) {
    return null;
  }

  const statBoxes = [
    { label: 'Number of drinks', value: stats.fileCount },
    { label: 'Average rating', value: stats.averageRating },
    { label: 'Top base spirit', value: stats.topSpirit },
    { label: 'Top glassware', value: stats.topGlassware },
  ];

  return (
    <div className='boxes'>
      {statBoxes.map(({ label, value }) => (
        <div className='statBox' key={label}>
          <div className='label'>
            <p>{label}</p>
          </div>
          <div className='value'>
            <p>{value}</p>
          </div>
        </div>
      ))}
    </div>
  );
}

export function Ratings() {
  const stats = useGlobalData()['drinks-stats']?.default;

  if (!stats) {
    return null;
  }

  const ratingLabels = {
    '★☆☆☆☆': 'Terrible',
    '★★☆☆☆': 'Poor',
    '★★★☆☆': 'Acceptable',
    '★★★★☆': 'Remarkable',
    '★★★★★': 'Outstanding',
  };

  return (
    <div>
      <h2>Ratings</h2>
      {stats.ratings.map(({ stars, percentage }) => (
        <p key={stars}>
          <span className='rating' title={ratingLabels[stars]}>
            {stars}
          </span>
          <span className='value percentage'>{percentage}%</span>
        </p>
      ))}
    </div>
  );
}

export function DrinksCards() {
  const cardBoxes = syrupsData;

  return (
    <div className='boxes'>
      {cardBoxes.map(({ name, ingredients }) => (
        <div className='cardBox syrupDetails' key={name}>
          <div className='label'>
            {name}
          </div>
          <div>
            {ingredients.map((line) => (
              <div key={line}>{line}</div>
            ))}
          </div>
        </div>
      ))}
    </div>
  );
}

# Drinks <img src='/img/drinks.png' height='48' className='titleIcon' />
<DrinksStats />

<br />
<p><a href='/drinks.csv' download>Download CSV</a></p>
<br />

<Ratings />

<br />

<details className='details'>
  <summary>Show syrup recipes</summary>
  <DrinksCards />
  <br />
  <p><a href='/syrups.json' download className='downloadLink'>Download JSON</a></p>
</details>