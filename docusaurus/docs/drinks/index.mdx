---
title: Drinks
---

import useGlobalData from '@docusaurus/useGlobalData';

export function DrinksStats() {
  const stats = useGlobalData()['drinks-stats']?.default;

  if (!stats) {
    return null;
  }

  const statBoxes = [
    { label: 'Number of drinks', value: stats.fileCount },
    { label: 'Average rating', value: stats.averageRating.toFixed(1) },
  ];

  const ratingLabels = {
    '★☆☆☆☆': 'Terrible',
    '★★☆☆☆': 'Poor',
    '★★★☆☆': 'Acceptable',
    '★★★★☆': 'Remarkable',
    '★★★★★': 'Outstanding',
  };

  return (
    <>
      <div className='boxes'>
        {statBoxes.map(({ label, value }) => (
          <div className='statBox' key={label}>
            <div className='label'>
              <p>{label}</p>
            </div>
            <div className='value'>
              <p>{value}</p>
            </div>
          </div>
        ))}
      </div>

      <br /><br />

      <div>
        {stats.ratings.map(({ stars, percentage }) => (
          <p key={stars}>
            <span className='rating' title={ratingLabels[stars]}>
              {stars}
            </span>
            <span className='value percentage'>{percentage}%</span>
          </p>
        ))}
      </div>
    </>
  );
}

export function DrinksCards() {
  const cardBoxes = [
    {
      title: 'Simple syrup',
      ingredients: ['1 part white sugar', '1 part water'],
    },
    {
      title: 'Demerara syrup',
      ingredients: ['1 part demerara sugar', '1 part water'],
    },
    {
      title: 'Agave syrup',
      ingredients: ['1 part agave syrup', '1 part water'],
    },
    {
      title: 'Honey syrup',
      ingredients: ['1 part honey', '1 part water'],
    },
    {
      title: 'Maple syrup',
      ingredients: ['2 parts maple syrup', '1 part water'],
    },
    {
      title: 'Raspberry syrup',
      ingredients: ['2 parts white sugar', '2 parts water', '1 part raspberries'],
    },
    {
      title: 'Grenadine',
      ingredients: ['100 parts white sugar', '100 parts pomegranate juice', '1 part orange blossom water'],
    },
    {
      title: 'Orgeat',
      ingredients: ['100 parts white sugar', '100 parts almond milk', '1 part orange blossom water'],
    },
    {
      title: 'Cream of coconut',
      ingredients: ['1 part white sugar', '1 part water', '2 parts coconut cream'],
    },
    {
      title: 'Saline solution',
      ingredients: ['1 part salt', '4 parts water'],
    },
  ];

  return (
    <div className='boxes'>
      {cardBoxes.map(({ title, ingredients }) => (
        <div className='cardBox' key={title}>
          <div className='label'>
            {title}
          </div>
          <div>
            {ingredients.map((line) => (
              <div key={line}>{line}</div>
            ))}
          </div>
        </div>
      ))}
    </div>
  );
}

# Drinks <img src='/img/drinks.png' height='48' style={{verticalAlign: 'top', marginTop: '0.35rem'}} />
<DrinksStats />

<br />

<details>
  <summary>Show syrup recipes</summary>
  <DrinksCards />
</details>